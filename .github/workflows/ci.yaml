# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO Continuous Integration (CI) GitHub Actions tests

name: Ultralytics Chinese Test CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  Chinese:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip' # caching pip dependencies
      - name: Install requirements
        shell: bash  # for Windows compatibility
        run: |
          python -m pip install --upgrade pip wheel
          pip install -e . --extra-index-url https://download.pytorch.org/whl/cpu
      - name: Check environment
        run: |
          yolo checks
          echo "RUNNER_OS is ${{ runner.os }}"
          echo "GITHUB_EVENT_NAME is ${{ github.event_name }}"
          echo "GITHUB_WORKFLOW is ${{ github.workflow }}"
          echo "GITHUB_ACTOR is ${{ github.actor }}"
          echo "GITHUB_REPOSITORY is ${{ github.repository }}"
          echo "GITHUB_REPOSITORY_OWNER is ${{ github.repository_owner }}"
          python --version
          pip --version
          pip list
      - name: Test
        shell: python
        run: |
          from ultralytics import YOLO
          
          import sys
          sys.stdout.reconfigure(encoding='utf-8')
          
          # Load a model
          model = YOLO("yolov8n.pt")  # load a partially trained model

          print('Printing1 name=检测项目1 ✅')
          name = '检测项目1'
          print(f'Printing1 name={name} ✅')
          
          # Train
          results = model.train(data="coco8.yaml", epochs=3, project="中文项目", name="检测项目1")
